<table class="table">
  <thead>
    <th>Title</th>
  </thead>
  <tbody>
    <% @collections.each do |col| %>
      <tr>
        <td><%= link_to col.title, col %></td>
      </tr>
    <% end %>
  </tbody>
</table>
