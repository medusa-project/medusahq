= render 'single_layout' do
  = link_to 'Delete Repository', repository_path(@repository), method: :delete, 'data-confirm': 'Are you sure? This cannot be undone.', class: 'btn btn-danger float-right', 'aria-role': :button
  %br
  = render 'form'
